# PRD V5 对齐 — 实施任务计划 (TaskPlan)

**基于文档**: PRD V5（智利车队管理PRD 第5版）+ PRD V6 优化项
**执行日期**: 2026-06-15
**项目路径**: `weidu-fleet/`
**版本**: V5.1（PM 决策确认 + Step 级格式化）

---

## 范围声明（V4.1新增，V5.0扩展）

**本 TaskPlan 范围**: PRD V4→V5 差异项（C01-C13）+ PRD V6 优化项（OPT-1/OPT-2/OPT-3）的代码实现。

**V5.0 新增纳入项**:
- OPT-1: 维修管理移除编辑操作（审计日志映射 #38 删除）→ T8.2/T4.7 联动 ✅PM已确认
- OPT-2: 批量导入模块字段调整（可覆盖=车牌号/外观/车型/购车时间；不可覆盖=VIN码/设备ID/车龄/总里程数/电池版本/最后位置）→ T18 ✅PM已确认
- OPT-3: "报警"→"预警"命名统一（缩小范围：故障/电池枚举保持"报警"，仅改模块标题/列名/围栏枚举）→ T19 ✅PM已确认

**不在本次范围内**（属于基线能力，需另行审查）:
- RBAC 权限矩阵（管理员角色不可编辑/删除、新增用户管理员角色置灰等）
- 时区处理（智利冬令时 UTC-4 / 夏令时 UTC-3 DST 切换）
- 租户数据隔离（下级租户数据对上级不可见）
- 批量导入校验（5 类校验 + 失败明细 Excel 下载）
- 登录/修改密码异常文案（7 条提示）
- 资产删除前置校验（已关联设备不可删除）
- 围栏校验（半径>100km、99 点位上限、关闭时才可编辑/删除）
- 地理位置显示精度（仅至街道/路口级别）

---

## 一、V5.0→V5.1 修订说明（PM 决策确认 + Step 格式化）

| 编号 | V5.0 原文 | V5.1 修正 | 来源 |
|------|---------|---------|------|
| D1 | T19 全局"报警→预警"，含故障/电池枚举 | **PM 决策**: 故障/电池枚举值**保持"报警"**，T19 范围缩小：仅改模块标题/Tab标签/列名/围栏枚举，不改故障/电池类型名 | PM 确认 |
| D2 | T18.1 批量导入字段标注"待确认" | **填入 PRD V6 已确认值**: 可覆盖=车牌号/外观/车型/购车时间；不可覆盖=VIN码/设备ID/车龄/总里程数/电池版本/最后位置 | PRD V6 |
| D3 | T8.2 标注"需 PM 确认产品概述表含编辑" | **PM 确认**: 维修管理无编辑功能，以详细功能逻辑表为准。移除 PM 确认前置条件 | PM 确认 |
| D4 | T19.5/19.7 围栏 alertType 改名标注为"须原子性提交" | **确认执行**: types + mock + Fence.tsx + i18n 四文件一次性修改 | PM 确认 |
| D5 | 任务格式为描述式 | **格式化**: 所有任务拆解为 Step 1...N 极小执行步骤，每步含 ✅ 自测标准 | Review |

---

## 二、V4.1→V5.0 修订说明（PRD V6 优化项合并）

| 编号 | V4.1 原文 | V5.0 修正 | 来源 |
|------|---------|---------|------|
| V1 | 无批量导入字段调整任务 | 新增 T18: 批量导入模块可覆盖/不可覆盖字段及下载模板调整 | Spec OPT-2 |
| V2 | 无"报警→预警"全局命名统一任务 | 新增 T19: 全局 54+ 处"报警"→"预警"命名替换（PRD+i18n+代码+Spec） | Spec OPT-3 |
| V3 | T8.2 仅覆盖 Repair.tsx 编辑移除 | T8.2 补充：审计日志映射表 #38"编辑维修"条目删除（48→47条） | Spec OPT-1 |
| V4 | T4.7 审计 47 条为最终态 | T4.7 补充确认：T18/OPT-1 删除"编辑维修"后 47 条与 PRD 一致 | 联动 |
| V5 | T6.1/T8.3 标签含"报警" | 需与 T19 联动：所有"XX报警"标签统一改为"XX预警" | 联动 |
| V6 | 范围声明不含 V6 优化项 | 范围声明新增 V6 三项优化纳入说明 | 全局 |

---

## 三、V4.0→V4.1 修订说明（Review 报告修订）

| 编号 | V4.0 原文 | V4.1 修正 | 来源 |
|------|---------|---------|------|
| F1 | T6.3 一键报修 type 传 'fault'/'battery' | 改为 '故障类'/'电池类'，与 RepairItem.type 定义一致 | Review P2 |
| F2 | T8.1 删除回退仅改 mock 层 | 新增 UI 层强制刷新机制（Risk.tsx focus 事件） | Review P3 |
| F3 | T4.7 审计计数 "49→48，PRD 47按操作函数计数" | 重写: "48→47，差异唯一来源是编辑维修1条" | Review P1 |
| F4 | T3.2 无 SOC 命名口径声明 | 新增 SOC低报警↔SOC过低 映射声明 | Review P4 |
| F5 | T7.5 标注"低优先级交互差异" | 升级为正式任务: 预警位置默认收起+懒加载 | Review P5 |
| F6 | 无车辆详情 Tab 验证 | 新增 T17 车辆详情7-Tab对齐验证 | Review P6 |
| F7 | T8.2/T15 使用行号引用 | 改为符号名/语义定位 | Review P8 |
| F8 | 无范围声明 | 新增范围声明节（RBAC/时区/租户隔离等不在范围内） | Review P7/P9/P10 |
| F9 | T6.3 toast "已生成工单" | 改为"已生成维修工单" | Review P13 |
| F10 | T13.2 要求改 risk.repairing | 标注已合规（zh.ts 已是"维修中"） | Review P11 |
| F11 | T4.7 无 contentTpl 格式说明 | 补充"采用可读空格分隔，非PRD方括号格式"声明 | Review P12 |
| F12 | V2→V3 M3 条目残留 | 标注为已删除+V4勘误说明 | Review P14 |

---

## 四、V3→V4 修订说明

| 编号 | V3 原文 | V4 修正 | 原因 |
|------|---------|---------|------|
| S1 | T7.5 给驾驶预警模块添加风险等级筛选（高/中/低） | **删除 T7.5**。风险等级筛选属于驾驶报告模块，且枚举为"安全/低危/中危/高危司机"，代码已正确实现 | PRD V5 驾驶预警筛选仅含车牌号+时间+预警类型，无风险等级；驾驶报告筛选已有(L502-516) |
| M1 | T15 DrivingReportDetail 缺累计时长和平均车速 | 接口补充 `cumulativeHours` + `avgSpeed` 两个字段 | PRD V5 详情页数据指标含"累计行驶时长(h)"和"平均车速(km/h)" |
| M2 | T15 未处理详情页多余图表 | 新增子步骤：移除"驾驶时间段占比"环形图和"风险事件折线图" | PRD V5 详情页仅要求行驶里程趋势+行驶区域分布+改善建议，不含这两个图表 |
| M3 | T15 未覆盖硬编码统计值 | 新增子步骤：将 `value={42.5}` 和 `value={62}` 替换为 mock 动态数据 | Driving.tsx L417/L427 统计卡片硬编码 |
| I1 | T8.2 未标注 PRD 内部矛盾 | 补充备注：PRD 产品概述含"编辑"但详细功能无 | PRD 产品概述表与维修管理详细功能表不一致 |
| I2 | T3.3 未标注命名不一致 | 补充备注：PRD 车辆详情 tab 用"紧急报警"，驾驶预警模块用"预警" | 代码统一使用短名"预警" |
| I3 | T7 未覆盖预警位置交互差异 | V4.1 升级为正式任务：预警位置默认收起+懒加载（T7.5） | PRD 明确要求此交互行为，非可选优化 |
| O1 | T14 未验证电池监控顶部聚合指标 | T14 验证表新增"电池监控-顶部4项聚合指标" | PRD V5 定义平均SOC/平均温度/平均续航/低电量报警数 |
| O2 | T14 未确认审计日志筛选能力 | T14 验证表新增"日志审计-操作菜单多选筛选" | PRD V5 要求操作菜单多选 |
| O3 | T14 驾驶报告验证项不完整 | 更新为"详情页仅含2个PRD图表+统计值动态化" | 配合 M2/M3 |
| ~~P1~~ | ~~T4.7 未说明审计47 vs 48差异根源~~ | ~~已废弃~~ | V4.1 核查：原推理不成立，T4.7 已重写为"48→47，差异唯一来源是编辑维修1条" |

---

## 五、V2→V3 修订说明

| 编号 | V2 原文 | V3 修正 | 原因 |
|------|---------|---------|------|
| R1 | T4.7: 49条→删2条→47条（PRD V5规定47条） | 48条→**删1条→47条**（差异唯一来源是代码多出"编辑维修"1条） | V4.1 核查修正：PRD 一键报修本身即为2行，无计数差异 |
| R2 | T8 未覆盖维修状态筛选值不一致 | T8 新增 **8.4—维修状态筛选统一** 子步骤 | PRD筛选项表写"已完成"、功能逻辑表写"维修完成"、代码用"维修完成"，需统一明确 |
| M1 | T4.7 未提及审计contentTpl用昵称问题 | T4.7 新增 **审计模板邮箱替换** 要求 | PRD V5审计日志contentTpl应使用邮箱（如 `maria.g@stgo-transport.cl`）而非昵称（如 `maria_op`） |
| M2 | T15 缺少数据指标字段定义 | T15 补充行驶里程/区域分布/改善建议的具体字段和数据结构 | 仅有图表类型描述，缺mock数据函数签名和字段规格 |
| ~~M3~~ | ~~T7 缺少风险等级筛选子步骤~~ | ~~已删除~~ | V4 勘误：风险等级筛选属于驾驶报告模块，代码已正确实现，V3 错误添加 |
| M4 | T16 电池详情指标缺确认 | T16 补充详情页需展示的完整指标列表 | 仅描述了趋势图，未确认详情页其他指标是否已齐全 |
| I1 | T8.3 维修描述用PRD全称（如"温度差异报警"） | T8.3 维修描述 value 统一使用 T3.2 简称 key（如"温差报警"） | 维修描述的 option value 必须与 BatteryAlert.type 枚举值一致，否则数据无法匹配 |

---

## 六、V1→V2 修订说明

| 编号 | V1 原文 | V2 修正 | 原因 |
|------|---------|---------|------|
| E1 | T3 故障类型混入电池类型 | 故障9种/电池14种严格按PRD V5分离 | PRD V5 故障预警和电池预警是独立模块 |
| E2 | T10 要求改充电状态为"满电/充电故障/未知" | **取消T10**，当前代码已与PRD V5一致 | PRD V5: 未充电/准备充电/充电中/对方放电/故障&异常 = 现有代码 |
| E3 | T3 Vehicle.charging 改为"满电/充电故障/未知" | 改为与PRD V5一致的5种 | 同步BatteryMonitorItem.status |
| E4 | T5 说"从5项扩展为6项" | 代码已有6项，仅需替换标签为i18n全称 | Dashboard.tsx L62已有6个标签 |
| E5 | T8 遗漏编辑功能移除 | 新增移除handleEdit/编辑按钮/Modal条件 | PRD V5 维修管理无编辑操作 |
| E6 | 审计日志48→47条 | 实际48条（含编辑维修），需删除1条→47条（V4.1修正） | mock.ts L722-771共48条，PRD逐条计数47条 |
| F2 | 维修描述选项仅9个 | 扩展为23个（9故障+14电池） | 新建维修描述需覆盖全部预警类型 |
| R3 | 评分权重随预警类型变化 | 评分维度保持PRD规定的5项各20% | 评分规则和预警类型是独立维度 |

---

## 七、任务总览

| 优先级 | 任务ID | 描述 | 涉及文件数 | 预估复杂度 |
|--------|--------|------|-----------|-----------|
| P0 | T1 | 电池报警筛选Bug修复 | 1 | 低 |
| P0 | T2 | 风险状态筛选文案修正 | 1 | 低 |
| P1 | T3 | 类型系统扩展（含勘误） | 1 | 高 |
| P1 | T4 | Mock 数据全量更新 | 1 | 高 |
| P1 | T5 | Dashboard 图表标签替换 | 1 | 低 |
| P1 | T6 | Risk.tsx 全面适配 | 1 | 高 |
| P1 | T7 | Driving.tsx ADAS 重构 | 1 | 高 |
| P1 | T8 | Repair.tsx 删除回退 + 移除编辑 | 1 | 中 |
| P2 | T9 | 全局语言清理 | 2 | 低 |
| ~~P2~~ | ~~T10~~ | ~~Battery.tsx 充电状态对齐~~ | - | **取消（已合规）** |
| P2 | T11 | 轨迹保留时效 | 2 | 低 |
| P2 | T12 | 资产划拨筛选多选 | 1 | 低 |
| P2 | T13 | i18n 词条全量更新 | 2 | 中 |
| P2 | T15 | 驾驶报告详情页图表 | 1 | 中 |
| P2 | T16 | 电池监控详情页日均电耗趋势图 | 1 | 中 |
| P2 | T18 | 批量导入模块字段调整（V5.1已确认字段） | 2 | 中 |
| P2 | T19 | "报警"→"预警"命名统一（V5.1缩小：故障/电池枚举保持报警） | 6+ | 中 |
| P3 | T17 | 车辆详情台账 Tab 对齐验证（V4.1新增） | 1 | 中 |
| P3 | T20 | V5.0 全量回归验证（V5.0新增） | - | 中 |
| P3 | T14 | 构建验证 + 回归测试 | - | 中 |

---

## 八、逐项任务明细

### T1: [P0] 电池报警筛选 Bug 修复

**问题**: Risk.tsx L327-331 电池报警筛选下拉 value 使用英文 key（`"Low SOC"`, `"High Temp"` 等），而 mock 数据层（mock.ts L203）使用中文 key，导致**筛选完全失效**。

**文件**: `src/pages/Risk.tsx`

**步骤**:
1. L327-331: 将5个 `<Option>` 的 value 改为中文（此步骤将在T6中被14种完整列表覆盖，可跳过直接做T6）

**验证**: 选择每种电池类型后表格数据正确过滤

---

### T2: [P0] 风险状态筛选文案修正

**问题**: Risk.tsx L291 和 L341 状态筛选 `<Option value="WorkOrder">已生成工单</Option>`，PRD V5 要求显示"维修中"。

**文件**: `src/pages/Risk.tsx`

**步骤**:
1. L291: `<Option value="WorkOrder">已生成工单</Option>` → `<Option value="WorkOrder">维修中</Option>`
2. L341: 同上

**验证**: 故障报警和电池报警 Tab 的状态筛选下拉均显示"维修中"

---

### T3: [P1] 类型系统扩展

**文件**: `src/types/index.ts`

**3.1 — 故障类型扩展（4→9种）**
- L86: `export type FaultType4 = 'VDC' | 'CDCU' | 'BDCU' | 'ADAS';`
- 改为:
```ts
export type FaultType = 'VDC' | 'CDCU' | 'BDCU' | 'ADAS'
  | 'DC-DC温度' | 'DC-DC状态' | '驱动电机控制器温度' | '驱动电机温度' | '高压互锁状态';
```
- L80: `type: FaultType4;` → `type: FaultType;`
- 全局搜索 `FaultType4` 替换为 `FaultType`（影响 mock.ts L1 import、L182 类型标注）

> ⚠️ V1勘误：V1把电池类型（温度差异/电池高温/储能过压/储能欠压/SOC低）混入了故障类型。PRD V5明确：故障预警9种和电池预警14种是独立枚举，不可混合。

**3.2 — 电池报警类型扩展（5→14种）**
- L93: `BatteryAlert.type` 从5种扩展为14种：
```ts
type: 'SOC过低' | '电池高温' | 'SOC跳变' | '充电故障' | '温差报警'
    | '储能过压' | '储能欠压' | '单体过压' | '单体欠压' | 'SOC过高'
    | '储能不匹配' | '单体一致性差' | '绝缘报警' | '储能过充';
```

> 对应PRD V5原文：温度差异报警/电池高温报警/车载储能装置过压报警/车载储能装置欠压报警/SOC低报警/单体电池过压报警/单体电池欠压报警/SOC过高报警/SOC跳变报警/可充电储能系统不匹配报警/电池单体一致性差报警/车载储能装置类型过充/绝缘报警/充电故障报警
> 代码中使用简称，映射关系：温度差异→温差报警, 车载储能装置过压→储能过压, 车载储能装置欠压→储能欠压, 单体电池过压→单体过压, 单体电池欠压→单体欠压, 可充电储能系统不匹配→储能不匹配, 电池单体一致性差→单体一致性差, 车载储能装置类型过充→储能过充

> **命名口径统一声明（V4.1新增）**: PRD 中"SOC低报警"与代码 `'SOC过低'` 指同一类型。Dashboard "今日低电报警"聚合、风控页筛选、审计 contentTpl 均统一使用 `'SOC过低'`。PRD↔代码映射: SOC低报警 ↔ SOC过低

**3.3 — 驾驶预警类型收窄（string→6种联合类型）**
- L101: `type: string;` → `type: DrivingAlertType;`
- 新增类型:
```ts
export type DrivingAlertType = '对车一级预警' | '对车二级预警' | '对车AEB制动'
  | '对人一级预警' | '对人二级预警' | '对人AEB制动';
```

> ⚠️ PRD 命名不一致：PRD 车辆详情→驾驶预警记录 tab 用"对车二级**紧急报警**"，但驾驶预警模块用"对车二级**预警**"。代码统一使用短名"预警"（与驾驶预警模块一致）。

**3.4 — Vehicle.charging 扩展（3→5种）**
- L20: `charging: '充电中' | '未充电' | '未知';`
- 改为: `charging: '未充电' | '准备充电' | '充电中' | '对方放电' | '故障&异常';`

> ⚠️ V1勘误：V1写的是"充电中/未充电/满电/充电故障/未知"，这是错误的。PRD V5电池监控充电状态为：未充电/准备充电/充电中/对方放电/故障&异常，与BatteryMonitorItem.status一致。

**3.5 — RepairItem 新增来源字段**
- L218-228 RepairItem 接口新增:
```ts
sourceAlertId?: string;
sourceAlertType?: 'fault' | 'battery';
```

**3.6 — Lang 类型清理**
- L176: `export type Lang = 'zh' | 'en' | 'es';` → `export type Lang = 'zh' | 'en';`

**验证**: `tsc --noEmit` 编译通过（此阶段会报大量下游错误，属正常）

---

### T4: [P1] Mock 数据全量更新

**文件**: `src/api/mock.ts`

**4.1 — 故障报警扩展为9种（L182-198 区域）**
```ts
const faultTypes: FaultType[] = [
  'VDC', 'CDCU', 'BDCU', 'ADAS',
  'DC-DC温度', 'DC-DC状态', '驱动电机控制器温度', '驱动电机温度', '高压互锁状态'
];
const faultContents: Record<string, string> = {
  'VDC': 'VDC电机控制器错误码0xE3',
  'CDCU': 'CDCU数据链路错误',
  'BDCU': 'BDCU继电器卡滞故障',
  'ADAS': 'ADAS摄像头需要标定',
  'DC-DC温度': 'DC-DC变换器温度超过阈值',
  'DC-DC状态': 'DC-DC变换器工作状态异常',
  '驱动电机控制器温度': '驱动电机控制器温度超过阈值',
  '驱动电机温度': '驱动电机温度超过阈值',
  '高压互锁状态': '高压互锁回路状态异常',
};
```
- 数据量从 `slice(0, 12)` 扩展为 `slice(0, 18)` 以覆盖更多类型
- `i % 4` → `i % 9`

**4.2 — 电池报警扩展为14种（L201-213 区域）**
```ts
const batTypes: BatteryAlert['type'][] = [
  'SOC过低', '电池高温', 'SOC跳变', '充电故障', '温差报警',
  '储能过压', '储能欠压', '单体过压', '单体欠压', 'SOC过高',
  '储能不匹配', '单体一致性差', '绝缘报警', '储能过充'
];
const batContents: Record<string, string> = {
  'SOC过低': '电量低于20%',
  '电池高温': '电池温度过高',
  'SOC跳变': 'SOC突变',
  '充电故障': '充电异常中断',
  '温差报警': '电芯温差过大',
  '储能过压': '车载储能装置电压过高',
  '储能欠压': '车载储能装置电压过低',
  '单体过压': '单体电池电压过高',
  '单体欠压': '单体电池电压过低',
  'SOC过高': 'SOC超过上限',
  '储能不匹配': '可充电储能系统不匹配',
  '单体一致性差': '电池单体一致性差',
  '绝缘报警': '绝缘电阻过低',
  '储能过充': '车载储能装置过充',
};
```
- 数据量从 `slice(0, 8)` 扩展为 `slice(0, 20)` 以覆盖更多类型
- `i % 5` → `i % 14`

**4.3 — 驾驶预警改中文ADAS（L217 区域）**
```ts
const alertTypes = [
  '对车一级预警', '对车二级预警', '对车AEB制动',
  '对人一级预警', '对人二级预警', '对人AEB制动'
] as const;
```
- L225: `i % 6` 保持不变（刚好6种）

**4.4 — Vehicle 充电状态扩展（L27 区域）**
```ts
charging: (['未充电', '准备充电', '充电中', '对方放电', '故障&异常'] as const)[Math.floor(Math.random() * 5)]!
```

**4.5 — addRepairItem 签名扩展（L678 区域）**
- type 参数明确为 `'故障类' | '电池类'`（与 `RepairItem.type` 一致，而非 `string`）
- 新增参数: `sourceAlertId?: string, sourceAlertType?: 'fault' | 'battery'`
- 赋值到 newItem

**4.6 — 新增状态回退函数**
```ts
export function revertFaultAlertStatus(alertId: string) {
  const alert = faultAlertData.find(a => a.id === alertId);
  if (alert) alert.status = 'Pending';
}
export function revertBatteryAlertStatus(alertId: string) {
  const alert = batteryAlertData.find(a => a.id === alertId);
  if (alert) alert.status = 'Pending';
}
```
- 需要将 faultAlertData/batteryAlertData 提取为模块级可变变量（let）

**4.7 — 审计日志清理**
当前代码 L722-771 共 **48 条** AUDIT_OP_MAPPINGS，PRD L977-1024 逐条计数 **47 条**。

**差异唯一来源**: 代码多出"编辑维修"（L759）1条，PRD 无此操作。
一键报修在 PRD（序号33故障类 + 34电池类）和代码（L755+L756）中均为 2 行，无计数差异。

> ~~V4 旧说明（已废弃）~~：此前 V2/V3/V4 声称"PRD按操作函数计数(一键报修=1)，代码按审计模板计数(一键报修=2)"，经代码实测核查，此推理不成立。PRD 本身就将一键报修拆为 2 行。

**操作一：删除"编辑维修"**
- **L759**: 删除 `{ menu: '维修管理', function: '编辑维修', contentTpl: ... }` — PRD V5 维修管理无编辑操作
- 删除后得 **47 条**，与 PRD 逐条计数一致

**操作二：重命名"删除车辆资产"为"删除资产"**
- **L734**: 将 `{ menu: '业务管理', function: '删除车辆资产', contentTpl: ... }` 的 `function` 改为 `'删除资产'`
- contentTpl 同步修改为 `删除资产 ${maskVin('LJ8T7AD0000100003')}`
- 原因：PRD V5 审计表序号12为"删除资产"而非"删除车辆资产"，属于重命名而非删除

**操作三：审计模板邮箱替换**
当前审计 contentTpl 中使用昵称（如 `maria_op`、`sys_operator`），PRD V5 要求使用邮箱账号：
- L736: `'编辑用户 maria_op 信息'` → `'编辑用户 maria.g@stgo-transport.cl 信息'`
- L737: `'删除用户 temp_user@test.cl'` — 已是邮箱格式，保持不变
- L738: `'重置用户 maria_op 的密码'` → `'重置用户 maria.g@stgo-transport.cl 的密码'`
- L762: `'编辑用户 sys_operator 信息'` → `'编辑用户 sysop1@weidu.cl 信息'`
- L764: `'重置用户 sys_operator 的密码'` → `'重置用户 sysop1@weidu.cl 的密码'`
- 全局排查所有 contentTpl 中的用户标识，统一使用 `auditAccounts` 数组中的邮箱格式

> 注意：`auditNicknames` 数组仍保留用于审计日志 `nickname` 字段（操作人昵称），仅 `contentTpl` 中的用户引用需改为邮箱

> **contentTpl 格式说明（V4.1新增）**: PRD 审计模板使用【】方括号占位符（如"将车辆【脱敏VIN】从【划拨前租户】划拨至【划拨后租户】"），代码使用空格分隔的可读形式。本次保持代码可读形式，不强制对齐 PRD 方括号格式。

**4.8 — Dashboard 图表数据**
- 现有 `getDashboardStats` 已提供 todayAlerts/fenceAlerts/lowBatteryAlerts
- 图表数据已在 Dashboard.tsx 内联（6组ADAS数据），仅需替换标签（见T5）

**4.9 — getVehicleRepairs 类型/描述统一**
- L662: `types` 从 `['故障维修', '电池维修']` 改为 `['故障类', '电池类']`（与 RepairItem.type 一致）
- L663: `descs` 需随故障9种+电池14种扩展同步更新

**验证**: 各页面数据正确展示

---

### T5: [P1] Dashboard 图表标签替换

**文件**: `src/pages/Dashboard.tsx`

**现状**: L62 已有6个标签 `['FCW1', 'FCW2', 'AEB_V', 'PCW1', 'PCW2', 'AEB_P']`，6组数据，6色。数量正确但标签为缩写。

> ⚠️ V1勘误：V1说"从5项扩展为6项"是错误的，代码已是6项。

**步骤**:
1. L62: 标签替换为 i18n 全称:
```ts
labels: [
  t('dash.adas_v1'), t('dash.adas_v2'), t('dash.adas_aeb'),
  t('dash.adas_p1'), t('dash.adas_p2'), t('dash.adas_paeb'),
],
```
2. 数据和颜色保持不变（已正确）

**验证**: 首页柱状图X轴显示完整ADAS预警名称

---

### T6: [P1] Risk.tsx 全面适配

**文件**: `src/pages/Risk.tsx`

**6.1 — 故障类型标签扩展（4→9种）**
- L16-21: `faultTypeLabels` 从4项扩展为9项:
```ts
const faultTypeLabels: Record<string, string> = {
  'VDC': 'VDC故障报警',
  'CDCU': 'CDCU故障报警',
  'BDCU': 'BDCU故障报警',
  'ADAS': 'ADAS故障报警',
  'DC-DC温度': 'DC-DC温度报警',
  'DC-DC状态': 'DC-DC状态报警',
  '驱动电机控制器温度': '驱动电机控制器温度报警',
  '驱动电机温度': '驱动电机温度报警',
  '高压互锁状态': '高压互锁状态报警',
};
```
- L278-281: 故障筛选下拉新增5个 `<Option>`

> ✅ V5.1 PM确认: 故障枚举标签保持"XX报警"（如"VDC故障报警"），T19 不改这些值。

**6.2 — 电池类型标签扩展（5→14种）**
- L23-29: `batteryTypeLabels` 从5项扩展为14项
- L327-331: 电池筛选下拉从5个 Option 扩展为14个（使用中文 value，修复T1的Bug）

**6.3 — 一键报修传入来源信息**
- L92: `addRepairItem(record.plate, record.vin || '', '故障类', ...)` — 原值 `'fault'` 与 `RepairItem.type` 定义 `'故障类' | '电池类'` 不一致，**必须修正**
- L98: `addRepairItem(record.plate, record.vin || '', '电池类', ...)` — 原值 `'battery'`，同理修正
- 增加 sourceAlertId=record.id, sourceAlertType='fault'|'battery'
- 同时更新报警状态为 'WorkOrder': 在调用 addRepairItem 后，将对应 record.status 改为 'WorkOrder'
- toast 文案从 `已生成工单` 改为 `已生成维修工单`（与 PRD 报警状态"维修中"语义一致），对应 i18n key 同步更新

**6.4 — T2 已完成**

**验证**: 故障9种/电池14种筛选正确；一键报修后再删除维修，报警状态回退

---

### T7: [P1] Driving.tsx ADAS 重构

**文件**: `src/pages/Driving.tsx`

**7.1 — 预警类型映射替换**
- L32-38: `alertTypeLabels` 替换为:
```ts
const alertTypeLabels: Record<string, string> = {
  '对车一级预警': '对车一级预警',
  '对车二级预警': '对车二级预警',
  '对车AEB制动': '对车AEB制动',
  '对人一级预警': '对人一级预警',
  '对人二级预警': '对人二级预警',
  '对人AEB制动': '对人AEB制动',
};
```

**7.2 — 筛选下拉改中文**
- L345-349: Option value 从英文改中文，6个 Option

**7.3 — 风险事件折线图扩展**
- L250-293: `weeklyRiskData.datasets` 从5条线扩展为6条:
  - 对应6种ADAS预警等级
  - 第6条线: 对人AEB制动

**7.4 — 评分规则面板**
- L470-476: 保持PRD V5规定的5项评分维度（急加速/急减速/急转弯/疲劳驾驶/AEB制动），各权重20%

> ⚠️ 评分维度（5项）和预警类型（6种ADAS）是两个独立概念。评分维度按PRD V5 §风险等级&驾驶评分规则 保持不变。

**7.5 — 预警位置默认收起+懒加载（V4.1 升级为正式任务）**
PRD 明确要求（L574/L403）："默认收起位置信息，点击『查看位置』时查询展示位置信息"。

步骤:
1. 驾驶预警列表的"预警位置"列改为默认显示"查看位置"链接
2. 点击后调用逆地址解析（或从 mock 数据获取）并展开显示完整地址
3. 车辆详情→驾驶预警记录 tab 同理

> 此为 PRD 明确要求的交互行为，非可选优化项。
> ~~V4 旧标注（已废弃）~~：此前标注为"低优先级交互差异，待后续优化"，经 Review 核查，PRD 明确要求此交互，应为正式任务。

> ⚠️ V3勘误：V3 T7.5 错误地给驾驶预警模块添加了"风险等级筛选（高/中/低）"。PRD V5 中风险等级筛选仅存在于**驾驶报告**模块（安全/低危/中危/高危司机），代码 [Driving.tsx L502-516](file:///Users/Zhuanz1/Desktop/AI文件夹/苇渡-智利车队管理/weidu-fleet/src/pages/Driving.tsx#L502-L516) 已正确实现，无需额外任务。

**验证**: 列表显示中文ADAS名称；筛选正常（类型筛选）；折线图6条线；评分面板5项各20%

---

### T8: [P1] Repair.tsx 删除回退 + 移除编辑功能

**文件**: `src/pages/Repair.tsx`, `src/api/mock.ts`, `src/pages/Risk.tsx`

**Step 1**: 在 mock.ts 中新增 `revertFaultAlertStatus(alertId)` 和 `revertBatteryAlertStatus(alertId)` 函数；将 faultAlertData/batteryAlertData 提取为模块级 `let` 变量
- ✅ 自测: 函数可被 import

**Step 2**: 在 Repair.tsx 的 `handleDelete` 中，删除前查找 target，若有 `sourceAlertId` 则调用 revert 函数回退风控预警状态
- ✅ 自测: 删除维修记录后 console.log 确认风控数据 status 变回 'Pending'

**Step 3**: 在 Risk.tsx 中添加 `window` focus 事件监听，触发时重新调用 `getFaultAlerts()`/`getBatteryAlerts()` 刷新本地 state
- ✅ 自测: 从维修页面切回风控页面，数据自动刷新

**Step 4**: 删除 Repair.tsx 中 `handleEdit` 函数、编辑按钮、Modal 中 `editingItem` 条件判断、`handleCreate` 中编辑分支
- ✅ 自测: 操作列无编辑按钮；Modal title 仅显示"新建维修"

**Step 5**: 在 mock.ts AUDIT_OP_MAPPINGS 中删除 #38 "编辑维修" 条目（L759）
- ✅ 自测: AUDIT_OP_MAPPINGS.length === 47

**Step 6**: 扩展 Repair.tsx 新建维修描述选项——type='故障类' 时显示9种故障描述，type='电池类' 时显示14种电池描述（value=枚举key，label=PRD全称）
- ✅ 自测: 切换维修类型后，描述下拉联动显示对应选项

**Step 7**: 确认维修状态筛选显示"维修中/维修完成"（PRD 筛选项表"已完成"为笔误）
- ✅ 自测: 维修记录页状态筛选下拉仅含"全部/维修中/维修完成"

> ✅ PM 决策（V5.1）：维修管理无编辑功能，以详细功能逻辑表+审计映射表（47条无编辑维修）为准。

---

### T9: [P2] 全局语言清理

**9.1 — types/index.ts**
- L176: `Lang` 类型移除 `'es'`（已在 T3.6 完成）

**9.2 — i18n/index.ts**
- L5: 删除 `import es from './es';`
- L23: resources 移除 `es: { translation: es }` 条目

**9.3 — i18n/es.ts**
- 保留文件不删除（以备将来恢复），但不再被引用

**验证**: 语言切换仅显示中/英，切换功能正常

---

### ~~T10: [P2] Battery.tsx 充电状态对齐~~ — **取消**

**原因**: 当前代码 Battery.tsx L146-151 已有5种状态映射：
```ts
idle → 未充电, preparing → 准备充电, charging → 充电中, discharging → 对方放电, fault → 故障&异常
```
**与 PRD V5 完全一致**，无需修改。types/index.ts BatteryMonitorItem.status (L129) 的5种枚举也已正确。

---

### T11: [P2] 轨迹保留时效

**11.1 — utils/trajectory.ts**
- L8: `TRAJECTORY_RETENTION_DAYS = 10` → `= 30`
- L14/26/43: JSDoc "10天" → "30天"

**11.2 — Monitor.tsx**
- 搜索 "10" 相关的 UI 提示文案，改为30天

**验证**: 轨迹回放 DatePicker 可选30天内日期

---

### T12: [P2] 资产划拨筛选多选

**文件**: `src/pages/Biz.tsx`

**步骤**:
1. L144: `useState<string | undefined>` → `useState<string[] | undefined>`
2. L245-247: 筛选逻辑:
```ts
// 处理"未划拨"特殊选项
const filtered = assetData.filter(a => {
  if (!tenantAssetFilter || tenantAssetFilter.length === 0) return true;
  if (tenantAssetFilter.includes('__unassigned__')) {
    if (!a.tenant) return true; // 未划拨
  }
  const tenantFilters = tenantAssetFilter.filter(v => v !== '__unassigned__');
  if (tenantFilters.length > 0 && a.tenant) {
    return tenantFilters.includes(a.tenant);
  }
  return !a.tenant && tenantAssetFilter.includes('__unassigned__');
});
```
3. L308-315: Select 组件添加 `mode="multiple"`
4. 选项列表中新增 `{ value: '__unassigned__', label: '未划拨' }` 选项

> ⚠️ V1勘误：V1用空字符串 `''` 作为"未划拨"的value，会导致 `includes('')` 匹配异常。改用 `'__unassigned__'` 作为特殊标识。

**验证**: 资产列表归属租户筛选支持多选，含"未划拨"选项

---

### T13: [P2] i18n 词条全量更新

**文件**: `src/i18n/zh.ts`, `src/i18n/en.ts`

**13.1 — 新增词条**

| Key | zh | en |
|-----|----|----|
| `dash.adas_v1` | 对车一级预警 | Vehicle L1 Warning |
| `dash.adas_v2` | 对车二级预警 | Vehicle L2 Warning |
| `dash.adas_aeb` | 对车AEB制动 | Vehicle AEB Braking |
| `dash.adas_p1` | 对人一级预警 | Pedestrian L1 Warning |
| `dash.adas_p2` | 对人二级预警 | Pedestrian L2 Warning |
| `dash.adas_paeb` | 对人AEB制动 | Pedestrian AEB Braking |

**13.2 — 修改词条**

> ✅ 已合规（V4.1确认）：`risk.repairing` 在 zh.ts L152 已是"维修中"，无需修改。
> T2 的 `<Option value="WorkOrder">` 显示文案修正仍需在 Risk.tsx 中执行（UI 层硬编码，非 i18n key）。

**13.3 — Driving.tsx 中引用的 i18n key 更新**
- 原英文 key 替换为中文 ADAS 名称对应的 i18n key

**验证**: 中英文切换后所有页面文案正确

---

### T15: [P2] 驾驶报告详情页图表

**文件**: `src/pages/Driving.tsx`（详情页部分）

**PRD V5 要求**:
- **行驶里程趋势**: 折线图，X轴：周报=周一到周日/月报=当月每天；Y轴=km
- **行驶区域分布**: 柱状图，X轴=城市，Y轴=km
- **驾驶改善建议**: 文字建议内容

**数据字段规格**:
```ts
interface DrivingReportDetail {
  // 行驶里程趋势
  mileageTrend: { date: string; km: number }[];  // 周报7条/月报30条
  // 行驶区域分布
  regionDistribution: { city: string; km: number }[];  // 按城市聚合
  // 驾驶改善建议
  suggestions: string[];  // 文字建议列表
  // 汇总指标（PRD V5 §驾驶报告-详情页 数据指标）
  totalMileage: number;       // 累计行驶里程 km
  cumulativeHours: number;    // 累计行驶时长 h（V4新增）
  avgSpeed: number;           // 平均车速 km/h（V4新增）
  avgScore: number;           // 驾驶评分
}
```

**步骤**:
1. 在驾驶报告详情页新增/验证"行驶里程趋势"折线图
2. 新增/验证"行驶区域分布"柱状图
3. 新增/验证"驾驶改善建议"文本区域
4. 新增/验证顶部汇总指标卡片（累计里程/累计时长/平均车速/驾驶评分）
5. mock 数据：新增 `getDrivingReportDetail(vin: string, period: 'week'|'month')` 函数（如不存在）
6. **移除多余图表**（V4新增）：
   - 删除 `dayNightChartData` 变量及其引用的"驾驶时间段占比" Doughnut 图表 Card — PRD V5 无此图表
   - 删除详情页中 `weeklyRiskData` 引用的"风险事件" Line 图表 Card — 此图属于驾驶预警模块，不属于报告详情页（注意：驾驶预警列表页可能仍需保留此变量，仅删除详情页中的引用）
7. **统计值动态化**（V4新增）：
   - 累计时长 Statistic 卡片中的硬编码 `value={42.5}` → `value={reportDetail.cumulativeHours}` 或从 DrivingReport 动态计算
   - 平均车速 Statistic 卡片中的硬编码 `value={62}` → `value={reportDetail.avgSpeed}` 或从 DrivingReport 动态计算
   - 已正确使用 `detailReport.km` 和 `detailReport.score` 的 Statistic 卡片 ✅

**验证**: 详情页仅展示 PRD 规定的 2 个图表（里程趋势+区域分布）+ 改善建议 + 4 项汇总指标，无多余图表；统计值均为动态数据；周报/月报切换正确

---

### T16: [P2] 电池监控详情页日均电耗趋势图

**文件**: `src/pages/Battery.tsx`（详情页部分）

**PRD V5 要求**:
- 图表类型：折线图
- 计算规则：近30天每日耗电数据（kWh/100km）
- 公式：日均电耗 = 日放电量 / 日里程 × 100

**详情页完整指标确认**:
| 指标 | 当前代码状态 | 需新增/修改 |
|------|------------|------------|
| 电池健康度(SOH) | ✅ 列表已有 | 详情页确认展示 |
| 电池温度 | ✅ 列表已有 | 详情页确认展示 |
| 续航里程 | ✅ 列表已有 | 详情页确认展示 |
| 日均电耗 | ✅ 列表已有(L135-139) | 详情页新增趋势图 |
| 累计充电次数 | ✅ 列表已有 | 详情页确认展示 |
| 充电状态 | ✅ 列表已有5种 | 详情页确认展示 |
| SOC | ✅ 列表已有 | 详情页确认展示 |
| VIN/车牌 | ✅ 基础信息 | 详情页确认展示 |

**步骤**:
1. 在电池监控详情页新增/验证"日均电耗趋势"折线图
2. mock 数据：新增 `getDailyConsumptionTrend(vin: string)` 函数返回近30天数据
```ts
interface DailyConsumption {
  date: string;       // 'YYYY-MM-DD'
  consumption: number; // kWh/100km
  dischargeKwh: number; // 日放电量
  mileageKm: number;   // 日里程
}
```
3. X轴=日期，Y轴=kWh/100km
4. 确认详情页上述所有指标均已展示（对照上表逐项验证）

**验证**: 详情页折线图展示30天趋势；所有指标项完整展示

---

### T17: [P3] 车辆详情台账 Tab 对齐验证（V4.1新增）

**文件**: `src/pages/VehicleDetail.tsx`（或相关组件）

**PRD 车辆详情含 7 个 tab**:
| Tab | PRD 要求 | 验证状态 |
|-----|---------|---------|
| 风控预警记录 | 预警类型聚合展示 | 待验证 |
| 驾驶预警记录 | 6种ADAS+位置默认收起(T7.5) | 待验证 |
| 电池监控 | 全量指标+日均电耗趋势图(T16) | T16覆盖 |
| 充电记录 | 电压/电流/功率/前后电量/时长 | 待验证 |
| 行程记录 | 起终点默认收起 | 待验证 |
| 维修记录 | 类型/描述与T8.3一致 | 待验证 |
| 里程报表 | 日/周/月/年4种维度折线图 | 待验证 |

**步骤**: 逐 tab 核对字段/图表与 PRD 一致性，差异项标注或补任务。

**验证**: 7 个 tab 全部与 PRD 对齐或在"已知偏差"中声明

---

### T18: [P2] 批量导入模块字段调整（V5.1已确认字段）

**文件**: `src/pages/Vehicles.tsx`, `src/api/mock.ts`

**Step 1**: 在 Vehicles.tsx 中找到批量导入处理函数，将可覆盖字段赋值逻辑改为覆盖 车牌号/外观/车型/购车时间
- ✅ 自测: 导入含新车型+新购车时间的 Excel → 车辆列表中对应字段已更新

**Step 2**: 确认不可覆盖字段（VIN码/设备ID/车龄/总里程数/电池版本/最后位置）在导入时跳过
- ✅ 自测: 导入含不同总里程的 Excel → 车辆列表中总里程保持不变

**Step 3**: 在 mock.ts 的 importVehicles（或等效逻辑）中同步更新字段覆盖规则
- ✅ 自测: `npm run build` 编译通过

**Step 4**: 下载模板中确保包含 VIN码/车牌号/外观/车型/购车时间 五列
- ✅ 自测: 下载模板 → 打开 Excel 确认列名正确

---

### T19: [P2] "报警"→"预警"命名统一（V5.0新增，V5.1缩小范围）

> ✅ **PM 决策（V5.1 T19.0）**: 故障/电池枚举值**保持"报警"**。仅改模块标题/Tab标签/列名/状态文案/围栏枚举/驾驶名称。

**Step 1**: `src/types/index.ts` — FenceItem.alertType 改为 `'出栏预警' | '入栏预警'`（BatteryAlert.type 中 `'温差报警'` 等不动）
- ✅ 自测: `tsc --noEmit` 会报 Fence.tsx/mock.ts 类型不匹配（正常，Step 2-4 修复）

**Step 2**: `src/api/mock.ts` — 围栏 mock 数据 alertType 值改为 `'出栏预警'`/`'入栏预警'`；审计 L748 contentTpl `预警【出栏报警】`→`预警【出栏预警】`（故障/电池 mock 和审计 L755/L756 不动）
- ✅ 自测: mock 数据中围栏 alertType 为"出栏预警"/"入栏预警"

**Step 3**: `src/pages/Fence.tsx` — 列表/新建表单 Radio/详情中所有"出栏报警/入栏报警"→"出栏预警/入栏预警"
- ✅ 自测: 围栏页面新建弹窗 Radio 显示"出栏预警/入栏预警"

**Step 4**: `src/pages/Risk.tsx` — fenceTypeMap 改为 `'入栏预警'`/`'出栏预警'`；tab 标签改为"围栏预警/故障预警/电池预警"；列名"报警内容→预警内容"、"报警时间→预警时间"、"报警状态→预警状态"；状态筛选 placeholder 改为"预警状态"（faultTypeLabels/batteryTypeLabels 中的"XX报警"不动）
- ✅ 自测: 风控页面三个 tab 标签为"XX预警"；筛选下拉无"报警"文案

**Step 5**: `src/pages/Dashboard.tsx` — "今日低电报警"→"今日低电预警"；排行榜"低电报警"→"低电预警"
- ✅ 自测: 首页看板指标卡片和排行榜列名无"报警"

**Step 6**: `src/pages/Vehicles/DrivingTable.tsx` — "对车一级预报警"→"对车一级预警"/"对车二级紧急报警"→"对车二级紧急预警"/"对行人一级预报警"→"对行人一级预警"/"对行人二级紧急报警"→"对行人二级紧急预警"
- ✅ 自测: 车辆详情-驾驶预警 tab 显示"对车一级预警"等

**Step 7**: `src/pages/Vehicles/TripTable.tsx` — "低电量报警"→"低电量预警"（AlertTable.tsx 中23种"XX报警"不动）
- ✅ 自测: 行程记录 tab "预警次数"旁无"报警"文案

**Step 8**: `src/pages/Vehicles/RepairTable.tsx` — 通用文案"故障报警/电池报警"→"故障预警/电池预警"（枚举名不动）
- ✅ 自测: 维修记录 tab 通用描述无"报警"

**Step 9**: `src/i18n/zh.ts` + `en.ts` — 替换模块标题/列名/首页指标中的"报警"→"预警"（枚举翻译不动）
- ✅ 自测: 中英文切换后，模块标题/列名显示"预警"

**Step 10**: `src/components/Layout/Sidebar.tsx` — 菜单项"围栏报警/故障报警/电池报警"→"围栏预警/故障预警/电池预警"
- ✅ 自测: 侧边栏菜单无"报警"

**Step 11**: `Spec.md` — 替换模块标题/列名/菜单中的"报警"→"预警"（故障/电池枚举名不动）
- ✅ 自测: `grep "报警" Spec.md` 仅含故障/电池枚举

**Step 12（最终验证）**: `grep -r "报警" src/`
- ✅ 自测: 结果仅包含故障9种+电池14种枚举名（如"VDC故障报警"、"温差报警"等），无模块标题/列名/菜单/围栏残留

> ⚠️ **原子性**: Step 1-4（types+mock+Fence+Risk 围栏相关）必须一次性提交

---

### T20: [P3] V5.0 全量回归验证（V5.0新增）

**来源**: Spec OPT-1/OPT-2/OPT-3 联动验证

**步骤**:
1. `npm run build` — 确认零 TypeScript 编译错误
2. `npm run dev` — 启动开发服务器
3. 针对 V5.0 新增项逐页面检查:

| 页面 | V5.0 新增检查项 |
|------|---------------|
| 全局 | `grep -r "报警" src/` 结果仅含故障/电池枚举名，模块标题/列名/菜单/围栏无"报警"（T19 验证） |
| 首页看板 | "今日低电预警"指标名正确 + 排行榜"低电预警"列名正确 |
| 风控预警 | tab 标签为"围栏预警/故障预警/电池预警" + 所有列名无"报警" |
| 车辆详情-风控 | 23种预警名称保持"XX报警"（PM确认不改） |
| 车辆详情-驾驶 | 6种预警名称无"报警"后缀（对车/对人一级预警等已统一） |
| 围栏管理 | 类型枚举为"出栏预警/入栏预警" |
| 维修管理 | 无"报警"文案 + 无编辑按钮 + 审计日志无"编辑维修" |
| 批量导入 | 可覆盖字段（车牌号/外观/车型/购车时间）正确覆盖；不可覆盖字段（VIN/设备ID/车龄/总里程/电池版本/最后位置）保持不变 |
| 侧边栏 | 菜单项无"报警" |
| 审计日志 | 映射表 47 条，无"编辑维修"；围栏 contentTpl 用"出栏预警"；故障/电池 contentTpl 保持"报警类型" |

---

### T14: [P3] 构建验证 + 回归测试

**步骤**:
1. `npm run build` — 确认零 TypeScript 编译错误
2. `npm run dev` — 启动开发服务器
3. 逐页面目视检查:

| 页面 | 检查项 |
|------|--------|
| 首页看板 | 柱状图6个ADAS全称 + 8项数据指标 + 排行榜7列 + 地图气泡窗 |
| 风控预警-故障 | 9种类型筛选 + 状态显示"未处理/维修中/维修完成" |
| 风控预警-电池 | 14种类型筛选 + 筛选功能正常（不再失效） |
| 驾驶预警 | 6种中文ADAS名称 + 预警类型筛选（无风险等级筛选） |
| 驾驶报告 | 列表含风险等级列+筛选(安全/低危/中危/高危司机) + 详情页仅含PRD规定的2个图表(里程趋势+区域分布)+改善建议+4项动态指标(无多余图表) |
| 电池监控 | 充电状态5种中文名 + 日均电耗列 + **顶部4项聚合指标(平均SOC/平均温度/平均续航/低电预警数)** + 详情页趋势图+全部指标完整 |
| 维修管理 | 无编辑按钮 + 删除后状态回退 + 新建描述联动（value=枚举key） + 状态筛选"维修中/维修完成" |
| 轨迹回放 | 30天日期范围 |
| 业务管理-资产 | 归属租户多选 + 未划拨选项 |
| 日志审计 | **47条**操作类型，无"编辑维修"，"删除车辆资产"已改为"删除资产"，contentTpl使用邮箱 + **操作菜单筛选支持多选** |
| 语言切换 | 仅中/英 |

---

## 九、依赖关系图

```
T3 (类型) ──→ T4 (Mock) ──→ T5 (Dashboard标签)
                         ──→ T6 (Risk全面适配)
                         ──→ T7 (Driving ADAS)
                         ──→ T8 (Repair回退+移除编辑)
                         ──→ T15 (驾驶报告图表)
                         ──→ T16 (电池详情趋势图)
T1 (Bug修复) ── 无依赖，可独立执行（或被T6覆盖）
T2 (文案修正) ── 无依赖，可独立执行
T9 (语言清理) ── 无依赖，可独立执行
T11 (轨迹) ── 无依赖，可独立执行
T12 (资产筛选) ── 无依赖，可独立执行
T13 (i18n) ──→ 依赖 T5/T7 确定新 key
T18 (批量导入) ── 无依赖，可独立执行（V5.1字段已确认）
T19 (报警→预警) ──→ 依赖 T6/T8/T13 完成后执行（全局替换须在其他任务定稿后进行，V5.1缩小范围）
T17 (车辆详情Tab验证) ──→ 依赖 T7/T8/T16 完成后验证
T20 (V5.0回归) ──→ 依赖 T18/T19 完成
T14 (V4.1验证) ──→ 依赖 T1-T17 全部完成
```

**推荐执行顺序**:
1. **第一批（并行）**: T2, T9, T11, T12, T18（无依赖的快速修复）
2. **第二批**: T3（类型基础）
3. **第三批**: T4（Mock 数据）
4. **第四批（并行）**: T5, T6, T7, T8, T15, T16（页面适配）
5. **第五批（并行）**: T13（i18n 词条）, T17（车辆详情Tab验证）
6. **第六批**: T19（全局"报警→预警"替换，须在 T6/T8/T13 定稿后执行）
7. **第七批（并行）**: T14（V4.1 验证）, T20（V5.0 回归验证）

> T1 可被 T6.2 直接覆盖（电池筛选下拉会在T6中完整重写为14种中文Option），无需单独执行。

---

## 十、已合规模认（无需变更）

| 项目 | 状态 | 说明 |
|------|------|------|
| 围栏管理-自定义围栏地址 | ✅ | Fence.tsx 已显示 `— —` |
| 电池监控-日均电耗列 | ✅ | Battery.tsx L135-139 已有 |
| 电池监控-充电状态5种 | ✅ | Battery.tsx L146-151 已与PRD V5一致 |
| Dashboard-数据指标8项 | ✅ | Dashboard.tsx L130-213 已有8个Statistic卡片 |
| Dashboard-排行榜7列 | ✅ | Dashboard.tsx L102-109 已有7列 |
| Dashboard-地图气泡窗 | ✅ | Dashboard.tsx L267-281 已含VIN/车牌/企业/设备/速度/SOC/地址/时间 |
| 维修管理-分页配置 | ✅ | Repair.tsx L224-229 默认20条，支持10/20/50/100 |
| 维修管理-无编辑操作（OPT-1） | ✅ | PRD V6 与 PRD V5 详细功能表一致：无编辑。T8.2+T4.7 覆盖代码移除 |
| 审计日志-编辑维修删除（OPT-1） | ✅ | T4.7 已覆盖：删除 #38 编辑维修，48→47 条 |
| 驾驶预警模块-全模块用"预警" | ✅ | PRD 驾驶预警模块（L457-L487）已正确使用"预警"，T19 不改此模块 |

---

**文档版本**: V5.1
**修订说明（V4.0）**: 基于V3.0 Review报告修订。删除错误的T7.5风险等级筛选（已存在于驾驶报告模块）、T15补充累计时长/平均车速/移除多余图表/统计值动态化、补充PRD内部矛盾标注（维修管理编辑/紧急报警命名/预警位置交互）、T14验证表补全电池聚合指标+审计多选+驾驶报告详情、T4.7补充审计47vs48计数差异根源。
**修订说明（V4.1）**: 基于 Review 报告修订。P0: 修复一键报修 type 值不一致('fault'→'故障类')、状态回退 UI 刷新机制、审计计数理由重写(48→47条)。P1: SOC命名口径声明、预警位置交互升级为正式任务(T7.5)、新增车辆详情Tab验证(T17)、行号引用改为符号定位。P2: 新增范围声明节、toast文案更新。P3: T13.2标注已合规、contentTpl格式声明、V2→V3残留勘误清理。
**修订说明（V5.0）**: 合并 PRD V6 优化项 Spec。新增 T18（批量导入字段调整）、T19（"报警→预警"命名统一）、T20（V5.0 回归验证）。T8.2 补充审计日志 #38 删除联动。T4.7 确认编辑维修删除后 47 条与 PRD 一致。范围声明扩展纳入 OPT-1/2/3。章节重编号（一→九）。
**修订说明（V5.1）**: PM 决策确认 + Step 级格式化。D1: T19 缩小范围——故障/电池枚举保持"报警"，仅改模块标题/列名/围栏枚举。D2: T18.1 填入 PRD V6 已确认字段（可覆盖=车牌号/外观/车型/购车时间）。D3: T8.2 PM 确认无编辑功能。D4: 围栏 alertType 改名确认原子执行。D5: 章节重编号（一→十）。
