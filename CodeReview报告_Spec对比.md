# 代码实现 vs Spec/PRD 优化建议报告（V4）

> 审查范围：全量源代码 vs V1.0 Spec.md + V1.0 PRD(最新) + V1.1 PRD + V1.2 草稿-10.md  
> 项目地址：`/Users/Zhuanz1/Desktop/AI文件夹/苇渡-智利车队管理/weidu-fleet`  
> 审查维度：全局规则覆盖、对象完整、字段完整、状态流转、CRUD矩阵、异常场景、审计日志、UI规范

**已排除项（按用户要求）：**
- 车辆详情各tab不再重复展示VIN码/车牌号（左侧已有）
- 不实现地图聚合(cluster)和定时刷新
- 语言切换仅保留中文/英文，不加西班牙语
- 登录、修改密码保持现状不改动
- 权限矩阵本期不做
- 维修新建选车下拉VIN/车牌号为**明文**（不脱敏）

---

## 一、全局规则覆盖度（V1.2 草稿-10.md 四条规则，共4项问题）

### 1.1 VIN码脱敏规则（保留前6后4，中间7个`*`）

| # | 模块 | 状态 | 说明 |
|---|---|---|---|
| 1 | Dashboard 排行榜/地图 | ✅ | `maskPlate(v)` / `maskVin(v)` |
| 2 | 车辆管理 列表/详情 | ✅ | `maskVin(v)` |
| 3 | 资产划拨 列表/搜索 | ✅ | `maskVin(v)` + `matchVinSearch()` |
| 4 | 围栏管理 车辆配置页/添加弹窗 | ✅ | `maskVin(v)` |
| 5 | 风控预警 围栏/故障/电池 | ✅ | `maskVin(v)` |
| 6 | 驾驶行为 预警/报告 | ✅ | `maskVin(v)` |
| 7 | 电池管理 列表/充放电 | ✅ | `maskVin(v)` |
| 8 | 行程管理 列表/详情 | ✅ | `maskVin(v)` |
| 9 | 实时监控 车辆列表/气泡 | ✅ | `maskVin(v)` |
| 10 | 维修管理 列表/详情/选车下拉 | ✅ | 选车下拉为明文（按要求） |
| 11 | 数据导出文件 | ✅ | 按草稿§11不应脱敏（明文导出） |

### 1.2 车牌号脱敏规则（首尾各保留2位，中间`*`）

| # | 模块 | 状态 | 说明 |
|---|---|---|---|
| 1 | 所有主要页面 | ✅ | 全局 `maskPlate(v)` 覆盖良好，维修选车下拉明文（按要求） |

### 1.3 地理位置精度规则（最高至街道/路口，截断门牌号）

| # | 模块 | 状态 | 说明 |
|---|---|---|---|
| 1 | 车辆管理 列表"最后位置" | ✅ | `LocationPrivacy` 组件内调 `truncateLocation` |
| 2 | 车辆详情"最后位置" | ✅ | `LocationPrivacy` + `truncateLocation` |
| 3 | 行程管理 列表/详情 起终点 | ✅ | `LocationPrivacy` |
| 4 | 驾驶行为 预警位置 | ✅ | `LocationPrivacy` |
| 5 | 围栏管理 列表地址 | ✅ | `truncateLocation(v)` |
| 6 | 风控预警 围栏报警列表/详情 | ✅ | `LocationPrivacy` + `truncateLocation` |
| 7 | 实时监控 车辆列表位置 | ✅ | `truncateLocation(v)` |
| **8** | **Dashboard 地图Popup** | **❌** | `Dashboard.tsx` L240-252: Popup中无地址字段，草稿§1要求气泡中"实时地址信息必须应用【地理位置精度规则】"，**需增加地址字段并截断** |
| **9** | **围栏新建/编辑/查看 地图中心点回显地址** | **❌** | `Fence.tsx` L321: 地图点击回显为坐标 `${c[0].toFixed(4)}, ${c[1].toFixed(4)}`，草稿§4要求"自动回显的地址需截断门牌号，应用【地理位置精度规则】" |

### 1.4 检索匹配规则（支持明文+脱敏后可见字符匹配）

| # | 模块 | 状态 | 说明 |
|---|---|---|---|
| 1 | 车辆管理 搜索 | ✅ | `matchVinSearch` + `matchPlateSearch` |
| 2 | 资产划拨 搜索 | ✅ | `matchVinSearch` |
| 3 | 围栏管理 添加车辆弹窗 | ✅ | `optionFilterProp="label"` + label已脱敏 |
| **4** | **围栏管理 车辆配置页搜索** | **❌** | `Fence.tsx` L127-128: 使用 `v.vin.includes()` 直接匹配，未用 `matchVinSearch`/`matchPlateSearch` |

---

## 二、对象/类型完整性（types/index.ts vs PRD，共15项）

### 2.1 缺失的类型定义

| # | 缺失类型 | PRD定义 |
|---|---|---|
| 1 | `VehicleAlertRecord` | 车辆详情-风控预警记录tab（23种预警，每条生成独立记录） |
| 2 | `VehicleDrivingRecord` | 车辆详情-驾驶预警记录tab（6种预警：对车一级/二级/AEB、对人一级/二级/AEB） |
| 3 | `VehicleBatteryRecord` | 车辆详情-电池监控信息tab（SOC、健康度、温度、续航） |
| 4 | `TransferRecord` | 资产划拨记录（划拨时间、变更人、划拨前租户、划拨后租户） |

### 2.2 枚举值不一致

| # | 类型 | 代码枚举 | PRD枚举 |
|---|---|---|---|
| 5 | `FaultAlert.status` | `'Pending'\|'WorkOrder'\|'Fixed'` | `'未处理'\|'维修中'\|'维修完成'`（一键报修后为`'已生成工单'`） |
| 6 | `BatteryAlert.status` | `'Pending'\|'WorkOrder'\|'Fixed'` | `'未处理'\|'维修中'\|'维修完成'`（一键报修后为`'已生成工单'`） |
| 7 | `FaultAlert.type` | `FaultType24`（24种） | `'VDC故障'\|'CDCU故障'\|'BDCU故障'\|'ADAS故障'`（4种） |
| 8 | `BatteryAlert.type` | `'Low SOC'\|'High Temp'\|...`（英文） | `'SOC过低'\|'电池高温'\|'SOC跳变'\|'充电故障'\|'温差报警'`（中文5种） |
| 9 | `DrivingReport.level` | `'Safe'\|'Low'\|'Medium'\|'High'` | `'安全司机'\|'低危司机'\|'中危司机'\|'高危司机'` |
| 10 | `FenceItem.status` | `'active'\|'inactive'` | `'生效中'\|'未生效'` |
| 11 | `FenceItem.alertType` | `string` | `'出栏报警'\|'入栏报警'` |
| 12 | `RepairItem.status` | `'in_progress'\|'done'` | `'维修中'\|'维修完成'` |
| 13 | `RepairItem.type` | `'fault'\|'battery'` | `'故障类'\|'电池类'` |

### 2.3 字段缺失/多余

| # | 类型 | 问题 |
|---|---|---|
| 14 | `Vehicle` | 缺少 `deviceType`(设备类型)、`deviceModel`(设备型号)，PRD设备信息含此2字段 |
| 15 | `FenceAlert` | 缺少 `device`(设备ID)、`speed`(报警时车速)，PRD围栏报警详情含此2字段 |
| 16 | `DischargeRecord` | 多余 `energyConsumed` 字段（PRD放电记录无此字段） |

---

## 三、字段完整性（各页面 vs PRD，共16项）

### 3.1 Dashboard 首页看板

PRD要求8项指标卡片：车辆总数、在线数、离线数、今日里程、累计总里程、今日驾驶预警、今日围栏预警、今日低电报警。代码已实现全部8项 ✅

| # | 问题 | PRD要求 | 代码位置 |
|---|---|---|---|
| 1 | 地图Popup缺少「电池信息(SOC)」 | PRD: 实时位置地图展示"电池信息" | `Dashboard.tsx` L240-252 |
| 2 | 地图Popup缺少「实时地址」字段+截断 | V1.2草稿§1: 气泡中地址必须应用【地理位置精度规则】 | 同上 |

### 3.2 车辆详情

PRD要求车辆信息(VIN/车牌/车型/外观/购车时间/车龄/电池版本/总里程/最后位置) + 设备信息(设备ID/设备名称/设备类型/设备型号) + 7个tab(风控预警/驾驶预警/电池监控/充电记录/行程记录/维修记录/里程报表)

| # | 问题 | PRD要求 | 代码位置 |
|---|---|---|---|
| 3 | 左侧车辆信息缺少「车型」字段（当前有model但需确认展示） | PRD: 车辆信息含"车型" | `Vehicles.tsx` L377-394 |
| 4 | 设备信息中「设备类型」「设备型号」为硬编码常量 | PRD: 应取自车辆数据，当前代码写死`OBD-II`/`WD-T100` | `Vehicles.tsx` L400-401 |
| 5 | AlertTable 仅映射6种预警名称 | PRD: 风控预警记录含**23种**预警（VDC/CDCU/BDCU/ADAS故障 + 19种电池类报警） | `Vehicles/AlertTable.tsx` L6-13 |
| 6 | DrivingTable 缺少「预警内容(CAN解码)」列 | PRD: 驾驶预警记录含"预警内容：CAN数据解码得出" | `Vehicles/DrivingTable.tsx` L24-28 |

### 3.3 驾驶行为

| # | 问题 | PRD要求 | 代码位置 |
|---|---|---|---|
| 7 | 驾驶预警搜索缺少「选择时间」日期区间选择器 | PRD: 搜索含"选择时间(日期区间)" | `Driving.tsx` L252-277 |
| 8 | 驾驶报告列表缺少独立「风险等级」列 | PRD: 列表字段含"风险等级"（安全/低危/中危/高危司机） | `Driving.tsx` L106-148 |

### 3.4 实时监控

| # | 问题 | PRD要求 | 代码位置 |
|---|---|---|---|
| 9 | 实时位置搜索仅支持车牌 | PRD: 搜索"企业名称/VIN码" | `Monitor.tsx` L112-116 |
| 10 | 实时位置车辆列表缺少「VIN」「车辆状态」字段 | PRD: 字段含"车辆VIN、车辆状态(在线/离线)" | `Monitor.tsx` L85-101 |

> 注：轨迹回放左侧**应展示企业树**（按企业层级分组展示车辆），仅不支持点击企业批量选车，但支持单选企业下车辆，当前实现正确 ✅

### 3.5 围栏管理

| # | 问题 | PRD要求 | 代码位置 |
|---|---|---|---|
| 13 | 围栏搜索缺少「操作时间」时间选择器 | PRD: 搜索含"操作时间(精确到分钟,最长1年)" | `Fence.tsx` L343-353 |

### 3.6 租户管理

| # | 问题 | PRD要求 | 代码位置 |
|---|---|---|---|
| 14 | 租户列表缺少「联系人」列 | PRD: 字段含"联系人" | `Tenant.tsx` L100-130 |
| 15 | 租户搜索缺少「创建时间」区间选择器 | PRD: 搜索含"创建时间(区间)" | `Tenant.tsx` L140-167 |
| 16 | 新增租户表单缺少「企业地址」字段 | PRD: 企业地址为非必填字段 | `Tenant.tsx` L197-227 |

### 3.7 Topbar

| # | 问题 | PRD要求 | 代码位置 |
|---|---|---|---|
| 17 | 租户切换展示为Select下拉 | PRD: "平铺展示，按关联时间排序，最新在最下方" | `Topbar.tsx` L127-144 |

---

## 四、状态流转完整性（共7项）

| # | 模块 | 问题 | PRD要求 | 代码位置 |
|---|---|---|---|---|
| 1 | 故障报警 | 状态标签i18n映射为"未处理"/"维修中"/"维修完成" | PRD: "未处理"/"维修中"/"维修完成"；一键报修后应为**"已生成工单"** | `Risk.tsx` L140-143 |
| 2 | 电池报警 | 同上 | 同上 | `Risk.tsx` L191-195 |
| 3 | 故障报警→一键报修 | `handleRepairFault` 将状态改为`WorkOrder`，i18n显示"维修中" | PRD: 点击一键报修后状态应为**"已生成工单"**（非"维修中"） | `Risk.tsx` L63-68 |
| 4 | 围栏状态 | 使用 `'active'/'inactive'` | PRD: `'生效中'/'未生效'` | `Fence.tsx` L41 |
| 5 | 维修→报警映射 | `handleComplete` 仅改维修状态，**未同步更新关联报警状态** | PRD: 完成维修→报警状态同步变为"维修完成" | `Repair.tsx` L83-88 |
| 6 | 围栏删除 | 缺少**二次确认**弹窗 | PRD: "仅未生效状态可删除，删除需二次确认" | `Fence.tsx` L110-113 |
| 7 | 租户过期 | 完全未实现**服务过期**状态及影响 | PRD: 一级租户到期→该级及下级显示"服务过期" | `Tenant.tsx` |

---

## 五、CRUD矩阵完整性（共13项）

| # | 模块 | 缺失操作 | PRD要求 |
|---|---|---|---|
| 1 | 维修管理 | 缺少**编辑**操作 | PRD: 维修记录支持完成维修、删除记录 |
| 2 | 租户管理 | 删除无**二次确认** | PRD: 删除需二次确认 |
| 3 | 租户管理 | 已开通账号的租户**仍可删除** | PRD: 已开通管理员账号的租户不可删除 |
| 4 | 租户管理 | 编辑操作仅按钮无实际逻辑 | 无编辑表单/回显 |
| 5 | 租户管理 | 编辑时企业编码**未置灰** | PRD: 编辑时"企业编码：输入框置灰，不可修改" |
| 6 | 租户开通账号 | 缺少**邮箱已存在**判断分支 | PRD: 邮箱已存在→展示已有信息+追加角色 |
| 7 | 租户开通账号 | 缺少**复制信息到粘贴板**功能 | PRD: 复制登录邮箱+密码 → Toast"复制成功" |
| 8 | 资产划拨 | 资产列表缺少**删除**操作 | PRD: 资产支持删除(未关联设备,二次确认) |
| 9 | 资产划拨 | 删除无「已关联设备不可删」校验 | PRD: 已关联设备弹出"不可删除"提示 |
| 10 | 资产划拨 | 批量划拨未勾选时缺少Toast提示 | PRD: "请选择指定数据操作" |
| 11 | 业务用户 | 重置密码仅为按钮，无实际**两步流程** | PRD: 二次确认→重置→弹窗展示→复制 |
| 12 | 业务角色 | 删除缺少**区分关联/未关联用户**的不同提示 | PRD: 已关联/未关联用户有不同提示文案 |
| 13 | 系统管理 | 管理员角色可被编辑/删除 | PRD: 管理员角色不可删除/不可配置/仅查看 |

---

## 六、异常场景完整性（共11项）

| # | 模块 | 缺失场景 | PRD要求 |
|---|---|---|---|
| 1 | 租户新增 | 企业名称无**唯一性校验** | PRD: 重复时输入框下飘红提示 |
| 2 | 租户新增 | 企业编码无**唯一性校验**和格式限制 | PRD: 英文+数字, 唯一, 重复飘红 |
| 3 | 租户开通 | 邮箱格式错误文案不对 | PRD: "请输入正确格式的邮箱" |
| 4 | 维修新建 | 成功提示用通用文案 | PRD: "新建维修记录成功" |
| 5 | 业务用户 | 编辑邮箱无**唯一性校验**(飘红) | PRD: 邮箱已被使用→输入框下飘红 |
| 6 | 业务用户 | 管理员角色用户**角色框未置灰** | PRD: 管理员角色编辑时角色框置灰不可更改 |
| 7 | 业务用户 | 管理员角色用户**可被删除** | PRD: 管理员角色账号不可删除 |
| 8 | 业务角色 | 新增角色名称无**唯一性校验** | PRD: 重复飘红"角色已存在" |
| 9 | 业务角色 | 角色名称无**≤10字符**限制 | PRD: 长度10个字符 |
| 10 | 新增角色 | 无**数据权限**配置项 | PRD: 新增角色含数据权限(默认全部下级租户) |
| 11 | 围栏新建 | 自定义围栏提交时未校验**闭合** | PRD: "提交数据时需校验必填项与围栏点位是否闭合" |

---

## 七、审计日志（V1.2 PRD日志审计）

| # | 检查项 | 状态 | 说明 |
|---|---|---|---|
| 1 | 日志审计页面存在 | ✅ | `src/pages/AuditLog.tsx` 已实现 |
| 2 | 路由 `/sys/audit-log` | ✅ | `App.tsx` 已配置 |
| 3 | 侧边栏菜单 | ✅ | `Sidebar.tsx` 已配置 |
| 4 | 类型定义 `AuditLog` | ✅ | `types/index.ts` 已定义 |
| 5 | 搜索功能（账号/操作类型/时间/结果） | ✅ | 四项筛选均实现 |
| 6 | 事件描述中VIN/车牌脱敏 | ⚠️ | Mock数据生成时已脱敏(`mock.ts` L689/L696使用`maskVin`)，但 `AuditLog.tsx` 组件未对`description`字段做前端防御性脱敏 |
| 7 | 日志存储时效 | — | PRD要求180天保留，前端不涉及，由后端实现 |

**建议**：在 `AuditLog.tsx` 的 `description` 列 render 函数中增加正则检测，对17位VIN和车牌号做前端兜底脱敏

---

## 八、系统管理 UI 重构（参考示例图，共2大项）

### 8.1 用户管理 UI 重构

**当前问题**（`Sys.tsx` L124-232）：
- 搜索栏仅2个Input+搜索按钮，缺少：角色下拉筛选、创建时间区间选择器、重置按钮
- 表格无序号列，分页信息不完整
- 重置密码无两步流程（确认弹窗 → 展示新密码+复制信息）
- 删除无二次确认弹窗
- 编辑用户Modal中邮箱无唯一性校验飘红

**参考示例图要求**：
1. 搜索栏：用户名称(Input) + 登录邮箱(Input) + 角色(Select多选) + 创建时间(RangePicker) + 重置/查询按钮
2. 表格：序号 + 用户昵称 + 登录邮箱 + 角色(多角色用"、"分隔) + 创建时间 + 操作(重置密码/编辑/删除)
3. 分页：共X条记录 第X/X页 + 每页条数切换(10/20/50/100) + 跳至
4. 重置密码：确认弹窗(红字"重置密码后该账号旧密码将不可用，是否继续？") → 成功弹窗(绿字"用户密码已重置成功" + 昵称 + 邮箱 + 新密码 + "复制信息"按钮)
5. 删除确认弹窗：红字"删除账号后账号将不可用，是否继续？"
6. 编辑用户：邮箱已存在时输入框下方飘红"该邮箱已被使用，请更换"

### 8.2 角色管理 UI 重构

**当前问题**（`Sys.tsx` L236-318）：
- 角色展示为卡片网格，应改为左右分栏布局
- 无角色列表选择区域，缺少编辑/删除操作入口
- 权限配置仅为Modal中简单多选框，无树形结构
- 无功能权限/数据权限的Tab切换

**参考示例图要求**：
1. **左右分栏布局**：左侧角色列表(点击选中高亮)，右侧权限配置区
2. **左侧角色列表**：角色名称列表 + 选中高亮 + "新增角色" + 鼠标移入显示编辑/删除图标
3. **右侧权限配置**：
   - Tab："功能权限" / "数据权限"
   - 功能权限：树形结构(Tree + Checkbox)，层级展开/折叠
   - 数据权限：单选("仅本级"/"本级+下级"/"全部下级")
   - 底部："保存" / "取消"
4. **删除角色弹窗**：
   - 已关联用户："该角色已关联用户，删除后用户无相关权限，是否继续？"
   - 未关联用户："删除后数据无法恢复，是否继续？"

---

## 九、V1.2 脱敏合规总览（草稿-10.md 12个模块）

| 草稿章节 | 模块 | VIN脱敏 | 车牌脱敏 | 位置截断 | 检索匹配 | 问题数 |
|---|---|---|---|---|---|---|
| §1 首页看板 | Dashboard | ✅ | ✅ | ❌ Popup缺地址 | — | 1 |
| §2 资产划拨 | Biz | ✅ | — | — | ✅ | 0 |
| §3 车辆管理 | Vehicles | ✅ | ✅ | ✅ | ✅ | 0 |
| §4 围栏管理 | Fence | ✅ | ✅ | ❌ 编辑页地图 | ❌ 配置页搜索 | 2 |
| §5 风控预警 | Risk | ✅ | ✅ | ✅ | — | 0 |
| §6 驾驶行为 | Driving | ✅ | ✅ | ✅ | — | 0 |
| §7 电池管理 | Battery | ✅ | ✅ | — | — | 0 |
| §8 行程管理 | Trips | ✅ | ✅ | ✅ | — | 0 |
| §9 维修管理 | Repair | ✅(明文正确) | ✅(明文正确) | — | — | 0 |
| §10 实时监控 | Monitor | ✅ | ✅ | ✅ | — | 0 |
| §11 数据管理 | DataExport | ✅(明文正确) | ✅(明文正确) | — | — | 0 |
| §12 日志审计 | AuditLog | ⚠️ 依赖mock | — | — | — | 1 |

---

## 汇总统计

| 维度 | 问题数量 | 严重程度 |
|---|---|---|
| V1.2全局规则覆盖 | 4项 | **高** — 位置精度/检索匹配有遗漏 |
| 类型定义(types) | 16项 | **高** — 枚举/字段与PRD不一致 |
| 字段完整性 | 15项 | **中-高** — 多处字段缺失 |
| 状态流转 | 7项 | **高** — 核心业务逻辑错误 |
| CRUD矩阵 | 13项 | **中-高** — 多处操作缺失 |
| 异常场景 | 11项 | **中** — 校验和边界处理不足 |
| 审计日志 | 1项 | **低** — 需防御性处理 |
| 系统管理UI | 2项 | **高** — 需全面重构 |
| **合计** | **~69项** | -- |

---

## 优先修复建议（按优先级排序）

| 优先级 | 修复内容 |
|---|---|
| **P0** | 修复报警状态流转：一键报修后状态应为"已生成工单"（非"维修中"） |
| **P0** | 实现维修完成→报警状态同步更新为"维修完成" |
| **P0** | 补齐V1.2位置精度遗漏：Dashboard Popup增加地址+truncateLocation |
| **P1** | **系统管理UI全面重构**：用户管理搜索栏/分页/弹窗；角色管理左右分栏+权限树 |
| **P1** | 修复所有类型的中英文枚举不一致（状态/类型/风险等级等13项） |
| **P1** | AlertTable补齐23种风控预警名称映射 |
| **P1** | 围栏管理：车辆配置页搜索改用matchVinSearch；编辑页地图回显地址截断 |
| **P2** | 补齐各处缺失的搜索项、校验、确认弹窗 |
| **P2** | AuditLog事件描述增加前端防御性脱敏 |

---

*报告结束*
