- **状态管理**：使用 `zustand` 配合 `persist` 中间件实现客户端状态持久化。
- **国际化**：采用 `i18next` 与 `react-i18next`，通过本地JSON资源文件管理多语言文案。
- **HTTP客户端**：基于 `axios` 封装，内置请求/响应拦截器处理认证逻辑。
- **地图集成**：使用 `leaflet` 库，并通过CDN路径修复默认图标加载问题。