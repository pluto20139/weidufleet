- **入口层**：`main.tsx` 作为应用根节点，集成 `BrowserRouter`、`ConfigProvider`（多语言/主题）及全局样式。
- **路由层**：`App.tsx` 定义核心路由表，采用 `React.lazy` 实现页面级代码分割，并通过 `Suspense` 提供加载状态。
- **布局层**：`AppLayout` 组件封装了侧边栏与顶部导航的通用结构，通过 `Outlet` 渲染受保护的业务子路由。
- **状态驱动**：依赖 `useAppStore` 进行登录态校验与页面重定向逻辑控制，实现认证守卫。