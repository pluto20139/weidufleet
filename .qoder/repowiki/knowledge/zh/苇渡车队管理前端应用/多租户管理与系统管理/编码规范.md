- 所有页面组件均使用 `useState` 维护本地筛选条件和模态框状态，并通过本地变量（如 `allAssets`, `allTenants`）模拟后端数据源。
- 表单操作（新增/编辑）统一采用 Ant Design 的 `Form.useForm` 钩子，并在 `Modal` 的 `onOk` 事件中触发 `validateFields` 进行校验与提交。
- 列表页普遍采用“卡片式筛选栏”前置的设计模式，包含输入框、下拉选择器及搜索/重置按钮，下方紧跟数据表格。
- 角色与状态展示统一使用 `Tag` 组件配合预定义的颜色映射表（如 `roleColors`），以增强视觉辨识度。